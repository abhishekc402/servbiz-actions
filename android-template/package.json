{
  "name": "servbiz-android-shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Android app shell template, build tools, and the build worker.",
  "//": "Deliberately separate from the root package.json. The build host needs the Android toolchain and these two SDKs, not the React app's dependency tree.",
  "scripts": {
    "test": "node tools/test-spec-validation.mjs",
    "test:unit": "./gradlew :app:testReleaseUnitTest",
    "worker": "node tools/worker.mjs",
    "worker:once": "node tools/worker.mjs --once",
    "icons": "node tools/generate-icons.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@supabase/supabase-js": "^2.108.2"
  },
  "//engines": "22 rather than 20: @supabase/supabase-js builds a RealtimeClient during createClient() and requires a global WebSocket, which Node ships natively only from 22.",
  "engines": {
    "node": ">=22"
  }
}
